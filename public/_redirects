# Cloudflare Pages Redirects
# Redirect /home to root
/home / 301

# Cloudflare handles .html extension removal automatically if "Clean URLs" is enabled.
# We don't need explicit 200 rewrites for every page as it can cause loops.
